[pytest]
testpaths = tests
markers =
    smoke: critical tests for fast feedback
    regression: full regression suite
    ui: UI tests
    api: API tests
    e2e: end-to-end business flow tests
    contract: API contract/schema tests
    billing: billing and invoice tests
    payment: payment and dunning tests
addopts = -v